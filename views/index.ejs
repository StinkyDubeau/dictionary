<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dictionary</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="./styles/main.css" rel="stylesheet">
</head>
<body>
    <div class="flex-container">
    <div class="flex-item">

    <div class="card has-shadow">
        <h1>Dictionary</h1>
        <form method="post" action="/define">
            <label for="word">Enter a word:</label>
            <input id= word" name="userWord" type="text" class="form-control">
        </form>
    </div>

    <div class="card has-shadow">
        <h2><%= word %></h2>
        <% if(locals.definitions[0].shortdef) { %>
            <ol>
            <% for(var i = 0; i < definitions.length; i++) { %>
                <% if(definitions[i].shortdef[0]) {%>
                    <li><%= definitions[i].shortdef[0] %></li>
                <% } %>
            <% } %>
            </ol>
        <% } else { %>
            <p>Sorry, we don't have any definition(s) for that. Check whether "<%= word %>" exists on <a target="_blank" href="https://www.merriam-webster.com">Merriam Webster</a>. That's where we pull our data from.</p>
        <% } %>
    </div>

    <div class="card has-shadow">
        <p>Made in Ottawa 🇨🇦 Jake Dubeau <%=new Date().getFullYear()%></p>
        <p>This site is made possible by <a target="_blank" href="https://www.merriam-webster.com">Merriam Webster</a>'s free  <a target="_blank" href="https://dictionaryapi.com">API</a>.</p>
    </div>
    </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>